# torch is installed separately by setup_training_venv.sh with the correct
# CUDA index.  Do NOT add torch here — uv would re-resolve it from PyPI
# and overwrite the CUDA-specific build (e.g. cu126 → cu128).
transformers>=5.2.0
trl>=0.15.0
accelerate>=0.34.0
deepspeed>=0.15.4
datasets>=2.21.0
sentencepiece>=0.2.0
PyYAML>=6.0.1
# flash-attn requires separate install: uv pip install flash-attn --no-build-isolation
